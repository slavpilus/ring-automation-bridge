# Ring Configuration
RING_REFRESH_TOKEN=your_ring_refresh_token_here

# Optional: Comma-separated list of location IDs to monitor
# Leave empty to monitor all locations
# RING_LOCATION_IDS=

WEBHOOK_URL=https://your-n8n-instance.com/webhook/ring-events
WEBHOOK_AUTH_HEADER=
# Optional: If your webhook endpoint requires authentication
# Legacy support for old environment variable names
# N8N_AUTH_HEADER=Bearer your-auth-token

# Optional: Comma-separated list of event types to exclude from webhook
# Example: camera_status_update,device_found
EXCLUDED_EVENTS=

# Optional: Enable debug mode for verbose logging (true/false)
DEBUG=false

# Optional: Polling interval for Ring events in milliseconds (default: 10000)
POLLING_INTERVAL=10000
